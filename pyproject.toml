[project]
name = "genPDF"
version = "1.0.0"
description = "Minimalist PDF generation service with template substitution"
requires-python = ">=3.12"
dependencies = [
    "xhtml2pdf>=0.2.11",
    "reportlab>=4.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]